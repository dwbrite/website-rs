storage: s3
secrets:
  htpasswd: |-
    ${ registry_user }:${ bcrypt_pass }
  s3:
    accessKey: ${ access_key }
    secretKey: ${ secret_key }
s3:
  region: ${ region }
  regionEndpoint: ${ region_endpoint }
  secure: true
  bucket: ${ bucket }
persistence:
  deleteEnabled: true
